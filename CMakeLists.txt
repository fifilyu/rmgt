CMAKE_MINIMUM_REQUIRED(VERSION 2.6)

PROJECT(rmgt)

SET(CMAKE_RUNTIME_OUTPUT_DIRECTORY ${CMAKE_BINARY_DIR}/bin)

ADD_SUBDIRECTORY(src)

ADD_SUBDIRECTORY(test)

ENABLE_TESTING()

ADD_TEST(NAME common_unittest COMMAND common_unittest)
ADD_TEST(NAME HostCtx_unittest COMMAND HostCtx_unittest)